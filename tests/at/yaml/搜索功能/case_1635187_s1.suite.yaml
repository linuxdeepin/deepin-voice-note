name: 搜索功能(#115119)_case_1635187_s1_suite
app: deepin-voice-note
description: 覆盖xlsx搜索无结果场景：输入不存在关键字搜索无结果后退出搜索
module: 搜索功能(#115119)
tags: []
fast_fail: false
env_check: []
setup:
- action: session_start
  command: bash -lc 'exec ./tests/at/setup_voice_note_at.sh'
  wait: 6.0
teardown:
- action: session_stop
suites:
- id: case_1635187_s1
  name: 搜索无匹配结果后退出搜索
  description: 覆盖xlsx用例1635187：输入搜索关键字无结果，退出搜索后恢复主界面
  tags: []
  steps:
  - step_type: action
    action: session_start
    command: bash -lc './tests/at/scripts/focus_search_and_query.py ZZZ不匹配'
    wait: 1.0
  - step_type: action
    action: keyboard_press
    key: Escape
    wait: 0.5
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      name: VoiceNoteMainWindow
  - step_type: assert
    action: assert_element
    selector:
      name: NoteItemListView
  - step_type: assert
    action: assert_element
    selector:
      name: NoteContentWebView
