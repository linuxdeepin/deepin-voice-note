name: 搜索功能(#115119)_case_1635181_s1_suite
app: deepin-voice-note
description: 覆盖xlsx搜索Enter触发与清空搜索框场景：输入关键字Enter搜索后Esc退出恢复
module: 搜索功能(#115119)
tags: []
fast_fail: false
env_check: []
setup:
- action: session_start
  command: bash -lc 'exec ./tests/at/setup_voice_note_at.sh'
  wait: 6.0
teardown:
- action: session_stop
suites:
- id: case_1635181_s1
  name: 搜索框输入关键字Enter搜索后清空恢复
  description: 覆盖xlsx用例1635181：搜索框输入关键字按Enter键搜索，清空搜索框恢复之前状态
  tags: []
  steps:
  - step_type: action
    action: session_start
    command: bash -lc './tests/at/scripts/focus_search_and_query.py 文本'
    wait: 1.0
  - step_type: action
    action: keyboard_press
    key: Escape
    wait: 0.5
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      name: VoiceNoteMainWindow
  - step_type: assert
    action: assert_element
    selector:
      name: NoteItemListView
  - step_type: assert
    action: assert_element
    selector:
      name: NoteContentWebView
