# list-trailing-br

Category: display-fidelity

Summernote may leave a trailing `<br>` inside list items. Migration should not persist it as a hardBreak because ProseMirror renders it as extra vertical spacing between list rows.
