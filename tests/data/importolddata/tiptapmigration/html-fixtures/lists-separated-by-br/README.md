# lists-separated-by-br

Category: display-fidelity

A single legacy `<br>` between two lists should become one empty paragraph, not a paragraph containing hardBreak which ProseMirror renders as two blank lines.
