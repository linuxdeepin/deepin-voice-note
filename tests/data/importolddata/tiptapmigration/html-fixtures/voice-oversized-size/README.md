# voice-oversized-size

Category: degradation

R1: oversized-voice-size is an inline literal. voiceSize clamped to qint64 max (as double).

input.html is a sanitized placeholder; no real paths/voice content/accounts/names.
